<style>
  .navbar-gradient {
    background: linear-gradient(90deg, #e0e7ff 0%, #f0f9ff 100%) !important;
    /* Soft blue to white */
    backdrop-filter: blur(6px);
    box-shadow: 0 2px 12px rgba(99,102,241,0.08);
  }
  .student-avatar {
    width: 38px;
    height: 38px;
    border-radius: 50%;
    background: #6366f1;
    color: #fff;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 1.3rem;
    font-weight: bold;
    margin-right: 0.7rem;
    box-shadow: 0 2px 8px rgba(99,102,241,0.12);
  }
  .welcome-text {
    transition: color 0.3s;
  }
  .welcome-text:hover {
    color: #6366f1;
  }
</style>

<nav class="navbar navbar-expand-lg navbar-gradient navbar-animate px-4 shadow-sm">
  <a class="navbar-brand d-flex align-items-center" href="#">
    <span class="student-avatar me-2">
      <i class="fas fa-user-graduate"></i>
    </span>
    <span class="fw-bold text-primary">Student Dashboard</span>
  </a>
  <div class="ms-auto d-flex align-items-center">
    <span class="welcome-text text-dark me-3">Welcome, <%= user.name %></span>
    <!-- Example for future dropdown/profile menu:
    <div class="dropdown">
      <a class="btn btn-light dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
        <i class="fas fa-user"></i>
      </a>
      <ul class="dropdown-menu dropdown-menu-end">
        <li><a class="dropdown-item" href="#">Profile</a></li>
        <li><a class="dropdown-item" href="/student/logout">Logout</a></li>
      </ul>
    </div>
    -->
  </div>
</nav>